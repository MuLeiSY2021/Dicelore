# 总之，来抽卡吧（build）· 2026-07-06 dogfood

> build-eval 手动门首跑（`build-eval` skill 首次实战）。目的：验证团本构建链路端到端可用 + loregm 对话记录落盘 + 初评构建行为。**非完整团本产出**（第 2 轮超时中止，见下）。

## 对象
- 源 md：`docs/research/scraped/总之，来抽卡吧_67916530.md`（218 KB，双身份：源素材 + 黄金参照）
- 构建会话：`sid=gacha-eval`（真后端 `.dicelore-eval`，`fakeGm:false` glm-5.2，经 HTTP lore 面直连 = build-mcp 同路径）
- commit：**无**（构建未收口——第 2 轮超时，Draft 停在 manifest）
- 对话记录：`.dicelore-eval/sessions/lore/gacha-eval/gacha-eval_session.jsonl`（79 行）

## 链路验证（门 2 核心，全过）
- ✅ `put_material` 流式上传 218KB 源 → `materials/gacha-source.md`（不入 LLM 上下文）。
- ✅ `send_to_builder` REST 同步驱动真 loregm 到 turn_end 返回 `{turnId}`；领域级错误经 body `error` 透传（第 2 轮 `gm_timeout`，HTTP 仍 202——BE-lore-error-shape 正常、不吞）。
- ✅ **对话记录 jsonl 落盘全符合设计**：`{_:"turn",parentUuid:null,input:"我已上传…"}`(作者指令，根) → `{_:"msg",...tool_use/tool_result/result}`（loregm 每步）逐行 `uuid`/`parentUuid` 链成树 + `HEAD` 指针文件。**这就是「作者说了什么→loregm 干了什么」的可回退/可 eval 记录**。
- ✅ `get_draft` 检视面生效（REST-only 不回散文，靠它看 Draft 增量）。

## 逐维裁决（对照源 md）
- **[A-构建行为] 到位**（对 `dicelore-build-core`「先摸源、只声明不跑团」）——证据：jsonl 里 turn1 `Read×5/Grep×3/Bash×1`、turn2 `Read×13/Grep×3/Bash×2` **先摸源再落笔**；全程只调 `dicelore_build_*`（set_manifest）+ 文件工具，**无 `resolve_*`/`sheet_update`/`narrate` 运行时裁决工具**（命中只在教条正文，非真调用）。加载了 `build-pack` skill（`Skill×2`）。构建纪律干净。
- **[A-构建行为] 有洞**：turn2 **180s 超时中止**——loregm 摸源勤但**过度**（Read×13 通读大源），180s 预算耗尽、只重设了次 manifest 就被 abort，没推进到 prologue/world/npc。**对大源，构建 turn 的时间预算/摸源策略不匹配**。
- **[B-团本质量] 无法完整评**：Draft 仅 `manifest{name:"总之，来抽卡吧", id:"gacha-summon"}`——**manifest 名/id 忠于源**（对得上），但 prologue/world/npc/cards/front 全空 → **开不起局**（缺 prologue）。非保真问题，是构建**没跑完**（超时）。

## vs 真实案例（整体）
链路本身完整可用、对话记录与检视面都到位、loregm 构建纪律（摸源/只声明/validate 意识）对教条忠实；**但在 218KB 大源 + 180s turn 预算下，单轮推进不动实质内容**——首轮只出 manifest、次轮超时。要造出「开得起局、忠于 md」的完整团本，须解决 turn 预算/摸源效率。

## build_core_fix_hints
- **build-pack 摸源阶段**：教 loregm **先 `grep`/`wc` 定位 + 分块 targeted `Read`（带 offset/limit）**，而非通读整源；大源（>N 万字）先建目录/索引再按需读回，避免单轮耗尽预算在摸源。
- **一次一件更细粒度**：大源下，manifest/prologue 各自单轮、别期望一轮出多域；教条可提示「大源分更多轮、每轮聚焦更小」。

## findings 分流
- **A·措辞**（当轮可改 build-pack）：摸源策略——targeted grep+分块 read 替代通读；大源分更细轮次。
- **B·架构/配置**（记 backlog、别提示词硬磨）：**构建 turn 的超时预算**——GM turn 默认 180s（RT-1，为跑团调的），但 build turn（摸大源+多工具）合理耗时更长；应给 lore/build 路径**独立、更长的 turn 超时**（或按源大小自适应）。提示词再省也难在 180s 内摸完 218KB + 落多域。→ 记 [backlog-后端 RT-10]。
</content>
