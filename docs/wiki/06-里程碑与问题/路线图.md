# 路线图

> **本页职责**：未来工作的**有序批次**——「先还哪个、再还哪个」。**AI 维护，可随时重排**（区别于人工维护、过去时的 [里程碑.md](里程碑.md)）。
> **怎么读**：每批列「修哪些 fix / 提哪些 feat」，每项链接回三个分层 backlog 池（[前端](backlog-前端.md) / [后端](backlog-后端.md) / [core](backlog-core.md)）的对应条目。批次顺序 = 优先级（反复出现 + 随规模恶化 = 靠前）+ 依赖链（前置闸先做），**AI 维护、可重排**。

---

## 第一批 · 教条 + harness 闭环（meta 闸，最优先）

> **真 GM 接教条 + 真 eval harness 是一条线**：harness 要跑真 GM，真 GM 现靠 `openingPrompt` 内联教条 stopgap 兜底（`16969d4`）——不先把教条正式接进 `DiceGm`，harness 跑出的行为类结论仍带 stopgap 味。故**先接教条再建 harness**。不建 harness 则一切「行为类/措辞类」结论作废，继续跑 eval 只产污染数据。

- feat：真 GM 接 gm-core skill（`DiceGm` skillStage 真接、去 `openingPrompt` stopgap）+ `RUN_LIVE` 验证 → 见 [backlog-后端 · G-后端-gmcore](backlog-后端.md)。**✅ 达成**（gm-core staged 接入 + RUN_LIVE 验证）。
- feat：搭真 eval harness（**方案改：CC 经 play-mcp 连真后端当玩家+评估者**，非原"子代理当GM + mock玩家"；见 [ADR-0025](../05-决策记录-ADR/README.md)）→ 见 [backlog-core 主题F · F1](backlog-core.md)。**✅ 达成**（play-mcp 闭环 + RUN_LIVE 通路验证）。
- feat：终局「由谁敲、何时敲」纳入观测，harness 建好后才能测真 GM 收不收局 → 见 [backlog-core 主题F · F2](backlog-core.md)。**🟡 harness 建好，待多轮跑测收局**。

**闸的意义**：本批建好后，三池里所有 ⚠️（待真harness）项才可重新评定。

> **达成（2026-06-24）**：G-后端-gmcore ✅ + F1 ✅（方案改 play-mcp）+ F2 🟡（待多轮测收局）。meta 闸基本达成——三池 ⚠️（待真harness）项可重新评定。详见 [ADR-0025](../05-决策记录-ADR/README.md) + [reports](../../../reports/)。**附带修复**：后端 session db 路径对齐 core（种子丢失根因，见 ADR-0025 ②）。

---

## 第二批 · GM 工具面可见性（头号架构债的真正剩余）

> **原"头号架构债"大半已落地**：物理表 front/plotline/foreshadow/history、toolgen 引擎、补充改名**均已完成**（见 [backlog-core 主题A′ 进度](backlog-core.md)）。**真正的剩余是"工具面暴露"——GM 仍调不到以概念为单位的读**。依赖链：视图层(闸) → toolgen 接线 → 业务工具声明(dogfooding) → GM 可见。**已拍板走声明式 dogfooding**（守 spec DT-9「团本扩展框架零改动」契约）。

- feat：视图层投影落地（`npc`/`player`/`world`/`relation`/`tension_board` 命名视图）—— step②/③ 的**前置闸** → 见 [backlog-core 主题A′ ①](backlog-core.md)。
- feat：业务 MCP 工具暴露（front/plotline/foreshadow/tension_board 接进 `buildMcp`，走声明式 dogfooding）+ toolgen 接线（进 `createMcpServer`/import/dogfooding step③）→ 见 [backlog-core 主题A · A2-A5](backlog-core.md) + [主题A′ ②③](backlog-core.md)。
- feat：NPC 一等抽象（A1，state 的 npc kind 升一等表/工具）→ 见 [backlog-core 主题A · A1](backlog-core.md)。
- feat：构建工具补全（plotline/foreshadow/anchor 构建工具，随 dogfooding 带）→ 见 [backlog-后端 · H-build-tools](backlog-后端.md)。

---

## 第三批 · 收尾 fix（原第三批删空后仅剩）

> 原"在途线"大半已落地：Play 会话端点、团本构建 import（全域物化）**均已完成**。真剩三个小尾巴。

- fix：`narration_commit.seq` 语义债（进程内计数器→log 真 seq）→ 见 [backlog-后端 · G-后端-seq](backlog-后端.md)。
- fix：`GET /events` 重连补叙述 → 见 [backlog-后端 · G-后端-重连](backlog-后端.md)。
- feat：`createLoreApp` 接 skills（构建 skill 接进 LoreSession）→ 见 [backlog-后端 · H-lore-skill](backlog-后端.md)。

---

## 第四批 · 增强 / 顺手（随设计周期带）

> **P3 增强 / P5 未来池**——随设计周期或顺手带。

- feat：裁决/披露/终局增强 B7（带修正∧分级检定）/ C1（分级线索披露）/ E1·E2（终局判据 + 机制）→ 见 [backlog-core 主题 · 裁决/披露/终局](backlog-core.md)；E1 待第一批 harness 能验证再动，E2 需 ADR。
- fix/feat：组件7 前端 fast-follow 中的可即修项（导航置灰、build 页 i18n）顺手清；其余（BG3 动效、面板拖拽、token 逐字、About 版本号、构建助手工具痕迹）随相关后端能力到位再带 → 见 [backlog-前端 主题 · 组件7 前端](backlog-前端.md)。
- feat：可观测性 · 日志分级统一——先 [backlog-core O1](backlog-core.md) 抽**同构** `shared/logger`（core/后端/前端共用、`error/warn/info/debug` 分级 + sessionId/turnId 上下文，浏览器 + node 通用），再 [backlog-core O2](backlog-core.md) core 运行时接入 + [backlog-后端 O-后端](backlog-后端.md) 后端按分级细覆盖 + [backlog-前端 O-前端](backlog-前端.md) 前端浏览器侧覆盖（现三层运行时几乎全程零日志、排障盲区，随规模恶化 ✓✓）；不阻塞主线、随设计周期带。

---

## 推迟池（不进当前批）

各池的 🔮 未来池项明确推迟，不编排进当前批次：[backlog-前端 未来池](backlog-前端.md) / [backlog-后端 未来池](backlog-后端.md) / [backlog-core 未来池](backlog-core.md)。
维护类（[backlog-core M1 · wiki 整理](backlog-core.md)）由 `organize-wiki` skill 持续处理，不占批次。

---

> [里程碑.md](里程碑.md) 人工维护、AI 不动（过去时、已达成节点）；**本页 AI 维护**（未来时、有序批次、可重排）。
