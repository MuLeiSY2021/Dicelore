# 里程碑

> **本页职责**：项目走到哪了——**已达成**的大节点时间线。每条只一句 + 指向权威落点（ADR / 设计页 / commit）。**不放进行中/待办**（那些进 [问题总账](问题总账.md)）。
> **上游依赖**：无（横切）。**状态**：🚧 living（达成一个补一行）。

---

## 阶段一 · 设计层闭环（2026-05 末 → 2026-06-18）

wiki 01-04 全部定稿，推导链（业务→概念→架构→决策）闭合；决策沉淀为 [ADR-0001…0019](../05-决策记录-ADR/)。关键决策：

- **三层行为塑形模型（数据/行动/塑形 = L1/L2/L3）** — ADR-0002，全 wiki 组织主轴。
- **业务驱动设计 + 单向推导** — ADR-0004。
- **数据层四业务域（sheet/event/world/rule）** — ADR-0005（部分取代 0003）；AI 对 rule 只读。
- **全栈 TypeScript + better-sqlite3(FTS5)** — ADR-0006。
- **resolver 二轴模型 + 状态骰下沉（roll_value 并入 sheet_update）** — ADR-0007；行动层核心。
- **定位重述：易分发 + 骑 Claude Code（model-agnostic）+ npm/CLI 分发** — ADR-0008。
- **narrate 升格 stream + 一轮范式（=agent 回合）+ 输出层三流** — ADR-0009。
- **可见性模型（visible 列 + show 白名单 + reveal_once 快照 + 强制隐藏）** — ADR-0010。
- **time 不升格（留 sheet 钟 + skill 声明）** — ADR-0011。
- **Skills 焊进 `.claude/skills/`** — ADR-0012。
- **timer 泛化为 sheet watcher、从 hook 解绑（sheet_update 就地触发）** — ADR-0013。
- **常驻保证（CLAUDE.md 指针 + SessionStart 注入）+ L3 两档烈度** — ADR-0014。
- **团本构建台六连（MD+CSV 包 / 读写层 / 双门面 / 检索 / 构建 skill）** — ADR-0015。
- **PbtA 对齐：Agenda 议程层 + fail-forward 双边护栏 + Front/Clock 团本类型** — ADR-0016。
- **状态回滚 = 回合快照（checkpoint，非逆运算/非重放）** — ADR-0017。
- **组件7 玩家客户端立项（Agent SDK headless host / 一契约两壳）** — ADR-0018。
- **玩家闸控明骰（BG3 式，明/暗骰拆 L1 工具名）** — ADR-0019。

## 阶段二 · 实现层（2026-06-17 → 2026-06-21，均已合并 main）

- **组件1 内层能力库**：dice 引擎 / 四域 store / resolve 编排 / session / errors（`packages/core`）。
- **组件2 MCP 工具面**：21 个 `dicelore_*` 工具（stdio server），端到端可调。
- **monorepo 搬迁**：引擎 `src/` → `packages/core`（`@dicelore/core`），root 转薄 workspace 管理者。
- **组件3 Skills 包**：常驻 `dicelore-gm-core` + 流程库 `dicelore-flow-{gacha,contest,anka,explore}`，焊进 `.claude/skills/`。
- **组件4 adapter + 输出层**：`dicelore init` + 三承重 hook（SessionStart/UserPromptSubmit/Stop）+ L3 两档 + 呈现模型生成器。
- **玩家闸控明骰 core 侧**：`pending_roll` / `commitPendingRoll` / roll-gate 接缝 + `resolve_*_open/_hidden`（ADR-0019）。
- **Skills eval-loop 工装**：`buildPlayerView` / `eval/assertions` / `eval/run.ts` / `eval/grade.ts` + 4 场景对标真人语料 + `grader.md`。
- **组件7 v1 非阻塞竖切**：`packages/shared` 契约 / core barrel / orchestrator 只读 REST + `presentation.ts` / `apps/web` 墨金主题骨架（193 测试绿）。
- **组件7 实时引擎面 Phase 1**：`AgentSdkDriver`（in-process 挂 MCP）/ `SessionHost` / WS 流 / 动作进 / **单人明骰 gate** / 宕机恢复；Playwright 端到端通过（当前分支 `feat/orchestrator-live-engine`）。

## 阶段三 · 验证层（进行中）

- **长程 eval（每剧本一团本 × ~50 回合）**：兽人冒险剧本已跑完 ~50 回合 faithful 局（[报告](../../../packages/core/eval/reports/50turn/orc-hunt-report.md)），产出一批架构缺口。**方法论待修**——当前为单人自演（玩家+GM 同体），需改为 mock 玩家 ↔ 真 Claude-GM；详 [问题总账 主题F](问题总账.md)。抽卡/恶龙团两剧本未跑。
- **P0 nits 清账（2026-06-21，主题D 全清）**：eval 暴露的三个客观引擎 nit 即修——① ZodError 摊成字段级 `BAD_INPUT`（不再笼统 `INTERNAL`）；② `world_search` 出参带 `rowid`（可直接喂 reveal_once/world_show）；③ 新增只读工具 `watcher_list`（GM 回顾未触发的钟/Front，补「只写不可读」缺口，亦是「未结张力看板」主题A 第一块拼图）。全量 208 测试绿、`tsc` 净。详权威 schema [MCP工具面](../04-子系统设计/MCP工具面.md)。
