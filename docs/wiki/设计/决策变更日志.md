# 决策变更日志

> **薄索引**：一行一条「日期 — 改了什么 — 链到哪页」。**只记时间线、不含设计内容**——「为什么这么设计」长在对应设计页的「决策与权衡」节里。历史决策原文归档已撤，决策权威在对应设计页「决策与权衡」节。

| 日期 | 决策 | 落点（设计页「决策与权衡」） |
|------|------|------|
| 2026-06-30 | wiki IA 重构：取消独立决策区、决策内嵌设计页、新增使用者指南轴 | 本结构本身 |
| 2026-07-01 | skill 加载改按引用（dice+lore local plugin + `skills`，退役 cpSync 暂存 + 废弃 `allowedTools:['Skill']`）；退役两侧内联教条兜底、加载失败 fail-loud、教条经 skill 单路径投递 | [后端双路径架构 · D3](04-子系统设计/后端双路径架构.md) |
| 2026-07-01 | 构建源摄入改会话工作区 + agentic 文件构建（流式素材上传），退役 BM25 检索 | [团本构建工具链 · D2](04-子系统设计/团本构建工具链.md) |
| 2026-07-01 | lore 构建 REST-only 的领域级 error 经 body `error` 字段回（HTTP 保持 202）；checkout ref=head 端点层解析 | [团本构建工具链 · D3](04-子系统设计/团本构建工具链.md) |
| 2026-07-02 | 单一数据根 `DICELORE_DATA_DIR`（废 `DICELORE_SESSIONS_DIR`/`DICELORE_CATALOG`）+ on-disk 布局 `$ROOT/{config.toml,catalog.db,keys.db,sessions/<kind>/<id>,logs}` + config.toml 结构化 `[env]` 节赋环境变量 + 程序↔数据分处；eval `$`=`.dicelore-eval` 为其 dev 实例 | [玩家客户端 · D5](04-子系统设计/玩家客户端.md) |
| 2026-07-02 | 会话对话记录抽进 `harness/runtime`（`SessionTranscript`：append-only UUID 树 + `HEAD` 指针，两侧共用只差工作目录）；transcript 铸全局唯一 UUID 作唯一权威、db 快照锚 `transcript_anchor`；rewind 做成 IoC 注册器（`Rewind`+`RollbackHook`，`rewindTo` 真移 HEAD=回退+分叉）；loregm 由此落 jsonl 对话记录 | [后端双路径架构 · D4](04-子系统设计/后端双路径架构.md) |
| 2026-07-02 | build 侧 eval skill（`build-eval`，play-eval 孪生）：真实案例 md 双身份驱动 loregm 建团本、读 jsonl 评构建行为+团本质量；`dicelore-eval`→`play-eval`；`eval-backend-setup` 共用前置 skill | [Skills-eval · D2](04-子系统设计/Skills-eval.md) |
