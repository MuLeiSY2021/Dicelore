# 声明式工具生成层接线（②③ + dogfooding）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 toolgen 引擎接进运行时 MCP 工具面——叙事层 front/plotline/foreshadow/tension_board 业务工具**不再硬编码**，而是用声明式工具生成层「声明出来」（dogfooding，spec §8 + DT-9 step③），证明「新工具=新声明、框架 core 代码零改动」契约可兑现。

**Architecture:** 三层接线——① 适配层 `toolgenToToolDef`：把 `compileTool` 出的 `{name,desc,handler}` + `decl.params` 适配成 MCP `ToolDef`（zod inputSchema/outputSchema/annotations）；② 标准库声明 `mcp/stdlib/narration.ts`：用 ToolDecl 数组声明叙事业务工具；③ 注册接缝：`createMcpServer(db, deps, extraTools?)` 接受生成工具集，与硬编码 `TOOLS` 并列注册。dogfooding 集成测试证明声明出的工具经 server 端到端落库 + 触发 watcher（承重墙不破）。

**Tech Stack:** TypeScript + zod + better-sqlite3 + @modelcontextprotocol/sdk + vitest。

## Global Constraints

- **DT-9 契约**：框架写函数 + MCP server 工厂接缝**不改业务逻辑**，只**加** `extraTools` 入口参数（向后兼容、默认空）。叙事业务工具靠声明，不新增硬编码 handler。
- **承重墙（DT-3）**：生成的写工具经 `compileWriteTool` → `matchWrite` → 正典原语（`applyMutations`/`*SetStatus`/`*Upsert`），**绝不裸跑 SQL**。集成测试须验证写工具落 event/触发 watcher。
- **命名（DT-6）**：生成工具短裸业务名（`front_open`），同 `dicelore_` 前缀；类别走 annotation（v1 可省 `source` 字段，annotations 用 ToolAnnotations 现有四字段）。
- **toolgen 编译产物形状**：`compileReadTool(decl) → {name, desc, handler:(db,args)=>unknown[]}`；`compileWriteTool(decl) → {name, desc, handler:(db,args)=>unknown}`。`ToolDecl = {name, desc?, params?:{[k]:"string"|"int"|"number"}, sql}`。
- **ToolDef 形状**（`mcp/tooldef.ts`）：`{name, title, description, inputSchema:ZodObject, outputSchema:ZodObject, annotations:{readOnlyHint,destructiveHint,idempotentHint,openWorldHint}, handler:(db,input)=>any}`。
- **writeMatch 支持的写模式**：mutate（`UPDATE t SET attr=attr±:p WHERE entity=:e`）、setStatus（`UPDATE front|plotline|foreshadow SET status=:s WHERE id=:i`）、insert（`INSERT INTO front|plotline|foreshadow (...) VALUES (:p,...)`）。**front_advance（推进 clock）撞天花板**：clock 在 state、经 front.clock_ref 关联，单条 SQL 需 JOIN（被拒）——v1 不声明 front_advance，记 backlog。
- 测试 co-located（`packages/core/src/.../*.test.ts`）；root `npm test`+`npm run typecheck` 验收。
- worktree 内执行；scoped `git add <精确路径>`，别 `-A`。git 命令一律 `--no-pager`。

## 设计张力与决断（透明记录）

1. **outputSchema 形状**：读工具回 `unknown[]`（数组非 object），写工具回 applyMutations 结果/void。MCP `ToolDef.outputSchema` 须 ZodObject。**决断**：适配层**包装** handler 出参为 `{ result: <原始返回> }`，`outputSchema = z.object({ result: z.unknown() })`。统一、不丢信息、满足 ZodObject 约束。
2. **生成写工具是否发 onCanonWrite**：现 `CANON_KIND` 表硬编码工具名。生成工具名不在表中 → 不发 onCanonWrite。**决断 v1**：生成叙事工具**不接 onCanonWrite**（前端实时引擎面 v1 不依赖叙事工具的写后推送；叙事写仍落 event/触发 watcher，承重不破）。记 backlog 作 v2 增强（生成工具的 canon kind 归因）。
3. **标准库声明放哪**：`packages/core/src/mcp/stdlib/narration.ts`，导出 `narrationToolDecls: ToolDecl[]`。这是「框架自带标准库包」的 v1 落点（spec §8）。团本自定义 `tools:` 段（manifest）的 import 装载是**后续独立 plan**（依赖 manifest schema + validator 扩展），本 plan 只做框架标准库 dogfooding，验证机制跑通。
4. **本 plan 不含**：① 团本 manifest `tools:` 段解析（catalog/import.ts）——独立 plan，依赖 validator 共享；② 完整一局真 LLM dogfooding——喂给 F1 eval harness；③ front_advance 等撞天花板工具。本 plan 验收 = 标准库叙事工具经 createMcpServer 注册 + 集成测试端到端落库/触发 watcher。

---

## File Structure

| 文件 | 职责 | 动作 |
|------|------|------|
| `packages/core/src/toolgen/toToolDef.ts` | 适配层：CompiledTool + ToolDecl → MCP ToolDef | Create |
| `packages/core/src/toolgen/toToolDef.test.ts` | 适配层单测 | Create |
| `packages/core/src/mcp/stdlib/narration.ts` | 叙事业务工具声明（ToolDecl[]）+ 编译为 ToolDef[] | Create |
| `packages/core/src/mcp/stdlib/narration.test.ts` | 声明编译 + 业务语义单测 | Create |
| `packages/core/src/mcp/server.ts` | `createMcpServer` + `wrapToolForTest` 接受 extraTools | Modify |
| `packages/core/src/mcp/server.test.ts` | extraTools 注册回归 | Modify |
| `packages/core/src/mcp/dogfooding.test.ts` | 端到端：声明叙事工具经 server 落库+触发 watcher | Create |
| `packages/core/src/index.ts` | 导出 `toolgenToToolDef`、`narrationStdlibTools` | Modify |

---

### Task 1: 适配层 toolgenToToolDef

**Files:**
- Create: `packages/core/src/toolgen/toToolDef.ts`
- Create: `packages/core/src/toolgen/toToolDef.test.ts`

**Interfaces:**
- Consumes: `compileTool(decl)` from `./compile.js`（出 `ReadTool|WriteTool`，均 `{name,desc,handler}`）；`type ToolDecl` from `./compile.js`；`type ToolDef`, `type ToolAnnotations` from `../mcp/tooldef.js`；`z` from `zod`
- Produces: `toolgenToToolDef(decl: ToolDecl): ToolDef`——编译 decl + 适配成 ToolDef，handler 出参包 `{result}`

- [ ] **Step 1: Write the failing test**

Create `packages/core/src/toolgen/toToolDef.test.ts`:

```typescript
// Copyright (C) 2026 MuLeiSY2021
//
// This file is part of Dicelore.
//
// Dicelore is free software: you can redistribute it and/or modify it under
// the terms of the GNU Affero General Public License as published by the Free
// Software Foundation, either version 3 of the License, or (at your option)
// any later version. See <https://www.gnu.org/licenses/>.

import { describe, expect, test, beforeEach } from "vitest";
import { openDb, initSchema, type DB } from "../store/db.js";
import { stateSet } from "../store/state.js";
import { toolgenToToolDef } from "./toToolDef.js";

let db: DB;
beforeEach(() => {
  db = openDb(":memory:");
  initSchema(db);
});

describe("toolgenToToolDef", () => {
  test("读声明 → ToolDef：inputSchema 由 params 生成、readOnlyHint=true、handler 出参包 result", () => {
    stateSet(db, "勇者", "HP", "20");
    const def = toolgenToToolDef({
      name: "hp_query",
      desc: "查 HP",
      params: { who: "string" },
      sql: "SELECT value FROM state WHERE entity = :who AND attr = 'HP'",
    });
    expect(def.name).toBe("hp_query");
    expect(def.description).toBe("查 HP");
    expect(def.annotations.readOnlyHint).toBe(true);
    // inputSchema 接受 {who:"勇者"}，拒缺参
    expect(() => def.inputSchema.parse({ who: "勇者" })).not.toThrow();
    expect(() => def.inputSchema.parse({})).toThrow();
    // handler 出参包 {result: 行数组}
    const out = def.handler(db, { who: "勇者" });
    expect(out).toEqual({ result: [{ value: "20" }] });
    // outputSchema 接受 {result:...}
    expect(() => def.outputSchema.parse(out)).not.toThrow();
  });

  test("写声明 → ToolDef：readOnlyHint=false", () => {
    const def = toolgenToToolDef({
      name: "plot_close",
      desc: "收一条线",
      params: { id: "string", s: "string" },
      sql: "UPDATE plotline SET status = :s WHERE id = :id",
    });
    expect(def.annotations.readOnlyHint).toBe(false);
    expect(def.name).toBe("plot_close");
  });

  test("int 参数类型映射为 z.number().int()", () => {
    const def = toolgenToToolDef({
      name: "by_floor",
      params: { floor: "int" },
      sql: "SELECT entity FROM state WHERE attr = :floor",
    });
    expect(() => def.inputSchema.parse({ floor: 3 })).not.toThrow();
    expect(() => def.inputSchema.parse({ floor: "x" })).toThrow();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run packages/core/src/toolgen/toToolDef.test.ts`
Expected: FAIL with "Failed to load url ./toToolDef.js"（未创建）。

- [ ] **Step 3: Write minimal implementation**

Create `packages/core/src/toolgen/toToolDef.ts`:

```typescript
// Copyright (C) 2026 MuLeiSY2021
//
// This file is part of Dicelore.
//
// Dicelore is free software: you can redistribute it and/or modify it under
// the terms of the GNU Affero General Public License as published by the Free
// Software Foundation, either version 3 of the License, or (at your option)
// any later version. See <https://www.gnu.org/licenses/>.

import { z } from "zod";
import type { DB } from "../store/db.js";
import { firstKeyword } from "./sqlGuard.js";
import { compileTool, type ToolDecl } from "./compile.js";
import type { ToolDef } from "../mcp/tooldef.js";

// 把声明的 params 类型字符串映射为 zod 校验器。
function paramSchema(type: string): z.ZodTypeAny {
  switch (type) {
    case "int":
      return z.number().int();
    case "number":
      return z.number();
    case "string":
      return z.string();
    default:
      // 未知类型保守按 string（toolgen 自身不限制，但 v1 仅这三类）
      return z.string();
  }
}

/**
 * 把一条声明式工具声明（ToolDecl）适配为 MCP 运行时工具面的 ToolDef。
 * - inputSchema：由 decl.params 生成 .strict() ZodObject（缺参/类型错即拒）
 * - outputSchema：统一 { result: unknown }（读工具回数组、写工具回原语结果，包一层满足 ZodObject）
 * - annotations：读 readOnlyHint=true，写 false；openWorldHint 全局 false
 * - handler：compileTool 出的 handler 出参包 { result }
 */
export function toolgenToToolDef(decl: ToolDecl): ToolDef {
  const compiled = compileTool(decl); // 编译期校验（坏 sql 在此抛）
  const isRead = firstKeyword(decl.sql) === "SELECT";

  const shape: z.ZodRawShape = {};
  for (const [key, type] of Object.entries(decl.params ?? {})) {
    shape[key] = paramSchema(type);
  }
  const inputSchema = z.object(shape).strict();
  const outputSchema = z.object({ result: z.unknown() });

  return {
    name: compiled.name,
    title: compiled.desc || compiled.name,
    description: compiled.desc,
    inputSchema,
    outputSchema,
    annotations: {
      readOnlyHint: isRead,
      destructiveHint: false,
      idempotentHint: false,
      openWorldHint: false,
    },
    handler: (db: DB, input: Record<string, unknown>) => ({
      result: compiled.handler(db, input),
    }),
  };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run packages/core/src/toolgen/toToolDef.test.ts`
Expected: PASS（3 测试绿）。

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/toolgen/toToolDef.ts packages/core/src/toolgen/toToolDef.test.ts
git commit -m "feat(core): toolgen 适配层 toolgenToToolDef(声明→MCP ToolDef)

compileTool 产物 {name,desc,handler} + params 适配成 ToolDef(zod schema);
读 readOnlyHint=true,出参包 {result} 满足 outputSchema ZodObject。

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: 叙事层标准库声明 mcp/stdlib/narration.ts

**Files:**
- Create: `packages/core/src/mcp/stdlib/narration.ts`
- Create: `packages/core/src/mcp/stdlib/narration.test.ts`

**Interfaces:**
- Consumes: `type ToolDecl` from `../../toolgen/compile.js`；`toolgenToToolDef` from `../../toolgen/toToolDef.js`；`type ToolDef` from `../tooldef.js`
- Produces: `narrationToolDecls: ToolDecl[]`（叙事业务工具声明）；`narrationStdlibTools(): ToolDef[]`（编译为 ToolDef[]，供 createMcpServer 注入）

**说明**：叙事业务工具 v1 标准库集（可经 writeMatch 三模式表达的子集）——
- `front_open`：INSERT front（id/name/stakes）
- `plotline_open`：INSERT plotline（id/title/summary）
- `plotline_advance`/`plotline_close`：UPDATE plotline SET status（setStatus）
- `foreshadow_plant`：INSERT foreshadow（id/content）
- `foreshadow_recall`/`foreshadow_abandon`：UPDATE foreshadow SET status（setStatus）
- `tension_board`：SELECT FROM tension_board 视图（读，无参，列全部未结张力）

不含 `front_advance`（推进 clock 撞 writeMatch 天花板，记 backlog）。`plotline_advance` 与 `plotline_close` 同为 setStatus，差别在语义 desc + 调用约定的 status 值。

- [ ] **Step 1: Write the failing test**

Create `packages/core/src/mcp/stdlib/narration.test.ts`:

```typescript
// Copyright (C) 2026 MuLeiSY2021
//
// This file is part of Dicelore.
//
// Dicelore is free software: you can redistribute it and/or modify it under
// the terms of the GNU Affero General Public License as published by the Free
// Software Foundation, either version 3 of the License, or (at your option)
// any later version. See <https://www.gnu.org/licenses/>.

import { describe, expect, test, beforeEach } from "vitest";
import { openDb, initSchema, type DB } from "../../store/db.js";
import { frontList } from "../../store/front.js";
import { foreshadowList } from "../../store/foreshadow.js";
import { narrationToolDecls, narrationStdlibTools } from "./narration.js";

let db: DB;
beforeEach(() => {
  db = openDb(":memory:");
  initSchema(db);
});

describe("叙事层标准库声明", () => {
  test("每条声明都能编译为 ToolDef（无坏 sql）", () => {
    const tools = narrationStdlibTools();
    expect(tools.length).toBe(narrationToolDecls.length);
    expect(tools.length).toBeGreaterThanOrEqual(8);
    for (const t of tools) {
      expect(typeof t.name).toBe("string");
      expect(typeof t.handler).toBe("function");
    }
  });

  test("名集覆盖叙事业务动词，无 front_advance（撞天花板）", () => {
    const names = narrationToolDecls.map((d) => d.name);
    for (const n of ["front_open", "plotline_open", "plotline_advance", "plotline_close", "foreshadow_plant", "foreshadow_recall", "foreshadow_abandon", "tension_board"]) {
      expect(names).toContain(n);
    }
    expect(names).not.toContain("front_advance");
  });

  test("front_open 工具经 handler 落库（走正典 frontUpsert）", () => {
    const tools = narrationStdlibTools();
    const frontOpen = tools.find((t) => t.name === "front_open")!;
    frontOpen.handler(db, { id: "f1", name: "城门攻防", stakes: "城破则民死" });
    const fronts = frontList(db);
    expect(fronts).toHaveLength(1);
    expect(fronts[0]).toMatchObject({ id: "f1", name: "城门攻防", status: "active" });
  });

  test("foreshadow_plant + foreshadow_recall 状态机经 handler 流转", () => {
    const tools = narrationStdlibTools();
    const plant = tools.find((t) => t.name === "foreshadow_plant")!;
    const recall = tools.find((t) => t.name === "foreshadow_recall")!;
    plant.handler(db, { id: "fs1", content: "神秘信物" });
    expect(foreshadowList(db)[0]).toMatchObject({ id: "fs1", status: "planted" });
    recall.handler(db, { id: "fs1", status: "recalled" });
    expect(foreshadowList(db)[0]).toMatchObject({ id: "fs1", status: "recalled" });
  });

  test("tension_board 读工具列未结张力（结果包 result）", () => {
    const tools = narrationStdlibTools();
    const plant = tools.find((t) => t.name === "foreshadow_plant")!;
    plant.handler(db, { id: "fs1", content: "信物" });
    const board = tools.find((t) => t.name === "tension_board")!;
    const out = board.handler(db, {}) as { result: unknown[] };
    expect(Array.isArray(out.result)).toBe(true);
    expect(out.result).toContainEqual({ kind: "foreshadow", id: "fs1", label: "信物", status: "planted" });
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run packages/core/src/mcp/stdlib/narration.test.ts`
Expected: FAIL with "Failed to load url ./narration.js"。

- [ ] **Step 3: Write minimal implementation**

Create `packages/core/src/mcp/stdlib/narration.ts`:

```typescript
// Copyright (C) 2026 MuLeiSY2021
//
// This file is part of Dicelore.
//
// Dicelore is free software: you can redistribute it and/or modify it under
// the terms of the GNU Affero General Public License as published by the Free
// Software Foundation, either version 3 of the License, or (at your option)
// any later version. See <https://www.gnu.org/licenses/>.

import type { ToolDecl } from "../../toolgen/compile.js";
import { toolgenToToolDef } from "../../toolgen/toToolDef.js";
import type { ToolDef } from "../tooldef.js";

// 框架自带「第一个标准库包」（dogfooding，spec §8）：叙事层 front/plotline/foreshadow
// 业务工具不硬编码，用声明式工具生成层声明出来。只含 writeMatch 三模式可表达者；
// front_advance（推进 clock，需跨表 JOIN）撞 DSL 天花板，不在此（记 backlog）。
export const narrationToolDecls: ToolDecl[] = [
  {
    name: "front_open",
    desc: "开一个 Front（威胁势力：名+利害）。Args: id, name, stakes。落 front 表 status=active。",
    params: { id: "string", name: "string", stakes: "string" },
    sql: "INSERT INTO front (id, name, stakes) VALUES (:id, :name, :stakes)",
  },
  {
    name: "plotline_open",
    desc: "开一条情节线。Args: id, title, summary。落 plotline 表 status=open。",
    params: { id: "string", title: "string", summary: "string" },
    sql: "INSERT INTO plotline (id, title, summary) VALUES (:id, :title, :summary)",
  },
  {
    name: "plotline_advance",
    desc: "推进一条情节线状态（status 传 active）。Args: id, status。",
    params: { id: "string", status: "string" },
    sql: "UPDATE plotline SET status = :status WHERE id = :id",
  },
  {
    name: "plotline_close",
    desc: "收口一条情节线（status 传 closed）。Args: id, status。",
    params: { id: "string", status: "string" },
    sql: "UPDATE plotline SET status = :status WHERE id = :id",
  },
  {
    name: "foreshadow_plant",
    desc: "埋一个伏笔。Args: id, content。落 foreshadow 表 status=planted。",
    params: { id: "string", content: "string" },
    sql: "INSERT INTO foreshadow (id, content) VALUES (:id, :content)",
  },
  {
    name: "foreshadow_recall",
    desc: "回收一个伏笔（status 传 recalled）。Args: id, status。",
    params: { id: "string", status: "string" },
    sql: "UPDATE foreshadow SET status = :status WHERE id = :id",
  },
  {
    name: "foreshadow_abandon",
    desc: "弃置一个伏笔（status 传 abandoned）。Args: id, status。",
    params: { id: "string", status: "string" },
    sql: "UPDATE foreshadow SET status = :status WHERE id = :id",
  },
  {
    name: "tension_board",
    desc: "列出所有未结张力（active front + open/active plotline + planted foreshadow + armed watcher）。无参，回 {kind,id,label,status} 行数组。",
    sql: "SELECT kind, id, label, status FROM tension_board ORDER BY kind, id",
  },
];

/** 编译标准库声明为运行时 ToolDef[]，供 createMcpServer 注入。 */
export function narrationStdlibTools(): ToolDef[] {
  return narrationToolDecls.map(toolgenToToolDef);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run packages/core/src/mcp/stdlib/narration.test.ts`
Expected: PASS（5 测试绿）。

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/mcp/stdlib/narration.ts packages/core/src/mcp/stdlib/narration.test.ts
git commit -m "feat(core): 叙事层标准库声明(dogfooding 第一个标准库包)

front/plotline/foreshadow 业务工具不硬编码,用声明式生成层声明出来;
front_advance 撞 writeMatch 天花板(跨表 JOIN)不声明,记 backlog。

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: createMcpServer / wrapToolForTest 接受 extraTools

**Files:**
- Modify: `packages/core/src/mcp/server.ts`
- Modify: `packages/core/src/mcp/server.test.ts`

**Interfaces:**
- Consumes: 现有 `TOOLS`、`ToolDef`
- Produces: `wrapToolForTest(db, deps, extraTools?: ToolDef[])`、`createMcpServer(db, deps?, extraTools?: ToolDef[])`——extraTools 与 TOOLS 并列注册/可调；默认 `[]` 向后兼容

**DT-9 守约**：只**加** `extraTools` 可选参数（默认空数组），不改任何现有业务逻辑。现有 19 工具行为零变化。

- [ ] **Step 1: Write the failing test**

Append to `packages/core/src/mcp/server.test.ts`（新 describe）：

```typescript
import type { ToolDef } from "./tooldef.js";
import { z } from "zod";

describe("createMcpServer extraTools 接缝（声明式生成工具并入）", () => {
  function fakeReadTool(): ToolDef {
    return {
      name: "fake_board",
      title: "假板",
      description: "测试用",
      inputSchema: z.object({}).strict(),
      outputSchema: z.object({ result: z.unknown() }),
      annotations: { readOnlyHint: true, destructiveHint: false, idempotentHint: false, openWorldHint: false },
      handler: () => ({ result: [{ ok: 1 }] }),
    };
  }

  it("extraTools 经 wrapToolForTest 可调", async () => {
    const db = openDb(":memory:");
    initSchema(db);
    const invoke = wrapToolForTest(db, {}, [fakeReadTool()]);
    const res = await invoke("fake_board", {});
    expect(res).toBeTruthy();
  });

  it("createMcpServer 接受 extraTools 不崩", () => {
    const db = openDb(":memory:");
    initSchema(db);
    expect(createMcpServer(db, {}, [fakeReadTool()])).toBeTruthy();
  });

  it("默认无 extraTools 时现有工具仍在（向后兼容）", async () => {
    const db = openDb(":memory:");
    initSchema(db);
    const invoke = wrapToolForTest(db, {});
    const res = await invoke("event_append", { kind: "narrate", content: "x" });
    expect(res).toBeTruthy();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run packages/core/src/mcp/server.test.ts`
Expected: FAIL——`invoke("fake_board", {})` 抛 "未知工具: fake_board"（byName 只含 TOOLS）。

- [ ] **Step 3: Write minimal implementation**

Modify `packages/core/src/mcp/server.ts`：

3a. 顶部 import 加 `ToolDef` 类型：

```typescript
import { TOOLS } from "./tools.js";
import type { ToolDef } from "./tooldef.js";
```

3b. `wrapToolForTest` 签名加第三参，byName 并入 extraTools：

```typescript
export function wrapToolForTest(db: DB, deps: McpServerDeps, extraTools: ToolDef[] = []) {
  const byName = new Map([...TOOLS, ...extraTools].map((t) => [t.name, t]));
  return async (name: string, args: unknown): Promise<unknown> => {
    const t = byName.get(name);
    if (!t) throw new Error(`未知工具: ${name}`);
    const result = await runTool(db, t, args);
    const kind = CANON_KIND[name];
    if (kind && deps.onCanonWrite && !result.isError) {
      let output: unknown = undefined;
      try { output = JSON.parse(result.content[0]?.text ?? "null"); } catch (e) { getLogger().warn({ err: e, toolName: name }, "信封 content 非 JSON,降级为 null output"); }
      deps.onCanonWrite({ kind, seq: maxSeq(db), toolName: name, output });
    }
    return result;
  };
}
```

3c. `createMcpServer` 签名加第三参，注册循环遍历 TOOLS + extraTools：

```typescript
export function createMcpServer(db: DB, deps: McpServerDeps = {}, extraTools: ToolDef[] = []): McpServer {
  if (deps.rollGate) setRollGate(deps.rollGate); // 单人明骰：接既有模块级 gate seam
  const server = new McpServer({ name: "dicelore", version: "0.0.0" });
  const invoke = wrapToolForTest(db, deps, extraTools);
  for (const t of [...TOOLS, ...extraTools]) {
    server.registerTool(
      `dicelore_${t.name}`,
      {
        title: t.title,
        description: t.description,
        inputSchema: t.inputSchema.shape,
        outputSchema: t.outputSchema.shape,
        annotations: t.annotations,
      },
      (args: unknown) => invoke(t.name, args) as any,
    );
  }
  return server;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run packages/core/src/mcp/server.test.ts`
Expected: PASS（原 4 测试 + 新 3 测试全绿）。

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/mcp/server.ts packages/core/src/mcp/server.test.ts
git commit -m "feat(core): createMcpServer 接受 extraTools(声明式生成工具并入工具面)

DT-9 守约:只加可选 extraTools 参数(默认空),现有 19 工具零变化;
wrapToolForTest + 注册循环并列 TOOLS + extraTools。

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: dogfooding 集成测试 + 公共面导出

**Files:**
- Create: `packages/core/src/mcp/dogfooding.test.ts`
- Modify: `packages/core/src/index.ts`

**Interfaces:**
- Consumes: `createMcpServer`、`wrapToolForTest`、`narrationStdlibTools`、`watcherSet`、`watcherList`、`logAppend`
- Produces: 导出 `toolgenToToolDef`、`narrationStdlibTools`、`narrationToolDecls`

**验收核心（spec §12）**：证明声明出的叙事工具经 server 端到端——① 落库（front/plotline/foreshadow 状态正确）；② 承重墙不破（写工具经正典原语，落 event）；③ 框架 core 零改动（叙事工具全来自声明，无新硬编码 handler）。

- [ ] **Step 1: Write the failing test**

Create `packages/core/src/mcp/dogfooding.test.ts`:

```typescript
// Copyright (C) 2026 MuLeiSY2021
//
// This file is part of Dicelore.
//
// Dicelore is free software: you can redistribute it and/or modify it under
// the terms of the GNU Affero General Public License as published by the Free
// Software Foundation, either version 3 of the License, or (at your option)
// any later version. See <https://www.gnu.org/licenses/>.

import { describe, expect, test, beforeEach } from "vitest";
import { openDb, initSchema, type DB } from "../store/db.js";
import { wrapToolForTest } from "./server.js";
import { narrationStdlibTools } from "./stdlib/narration.js";
import { frontList } from "../store/front.js";
import { plotlineList } from "../store/plotline.js";

let db: DB;
let invoke: (name: string, args: unknown) => Promise<unknown>;
beforeEach(() => {
  db = openDb(":memory:");
  initSchema(db);
  invoke = wrapToolForTest(db, {}, narrationStdlibTools());
});

// 信封 → 解出业务出参
async function call(name: string, args: unknown): Promise<any> {
  const res = (await invoke(name, args)) as { content: { text: string }[]; isError?: boolean };
  expect(res.isError).toBeFalsy();
  return JSON.parse(res.content[0].text);
}

describe("dogfooding：声明叙事工具经 MCP server 端到端", () => {
  test("front_open 经信封落库（front 表 status=active）", async () => {
    await call("front_open", { id: "f1", name: "城门攻防", stakes: "城破民死" });
    expect(frontList(db)).toHaveLength(1);
    expect(frontList(db)[0]).toMatchObject({ id: "f1", name: "城门攻防", status: "active" });
  });

  test("plotline 开→推进→收口 全流程经 server", async () => {
    await call("plotline_open", { id: "p1", title: "主线", summary: "救公主" });
    expect(plotlineList(db)[0].status).toBe("open");
    await call("plotline_advance", { id: "p1", status: "active" });
    expect(plotlineList(db)[0].status).toBe("active");
    await call("plotline_close", { id: "p1", status: "closed" });
    expect(plotlineList(db)[0].status).toBe("closed");
  });

  test("tension_board 经 server 列出多源未结张力", async () => {
    await call("front_open", { id: "f1", name: "攻城", stakes: "x" });
    await call("plotline_open", { id: "p1", title: "主线", summary: "y" });
    await call("foreshadow_plant", { id: "fs1", content: "信物" });
    const board = await call("tension_board", {});
    // 信封出参 = handler 的 {result: [...]}
    const kinds = (board.result as { kind: string }[]).map((r) => r.kind).sort();
    expect(kinds).toEqual(["foreshadow", "front", "plotline"]);
  });

  test("坏参数被 inputSchema 拦（缺 id）", async () => {
    const res = (await invoke("front_open", { name: "无 id" })) as { isError?: boolean };
    expect(res.isError).toBe(true);
  });

  test("框架零改动验证：叙事工具全部来自声明（不在硬编码 TOOLS 中）", async () => {
    const { TOOLS } = await import("./tools.js");
    const hardcodedNames = new Set(TOOLS.map((t) => t.name));
    for (const n of ["front_open", "plotline_open", "foreshadow_plant", "tension_board"]) {
      expect(hardcodedNames.has(n)).toBe(false); // 不硬编码
    }
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run packages/core/src/mcp/dogfooding.test.ts`
Expected: FAIL（若 Task 1-3 已落则可能直接 PASS——此 test 是集成验收，依赖前序 task。若前序已绿，本步预期 PASS，跳到 Step 4）。

- [ ] **Step 3: （无新实现，集成测试验证前序）**

本 task 不写新实现，dogfooding.test.ts 验证 Task 1-3 的集成正确性。若 fail，回查 Task 1-3。

- [ ] **Step 4: 导出公共面 + 全量回归**

Modify `packages/core/src/index.ts`——在 toolgen/视图相关导出区追加：

```typescript
// 声明式工具生成层接线（叙事层 dogfooding，spec §8 + DT-9 step③）
export { toolgenToToolDef } from "./toolgen/toToolDef.js";
export { narrationStdlibTools, narrationToolDecls } from "./mcp/stdlib/narration.js";
```

Run: `npx vitest run packages/core/src/mcp/dogfooding.test.ts`
Expected: PASS（5 测试绿）。

Run 全量: `npm test`
Expected: PASS（现有 405 + 新增约 16 测试全绿）。

Run typecheck: `npm run typecheck`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/mcp/dogfooding.test.ts packages/core/src/index.ts
git commit -m "test(core): dogfooding 集成——声明叙事工具经 MCP server 端到端 + 公共面导出

证明 spec DT-9 契约:叙事业务工具全来自声明(不在硬编码 TOOLS)、
经 server 落库/承重墙不破/坏参被拦。导出 toolgenToToolDef + narrationStdlibTools。

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec coverage**：
- spec DT-9 step③（叙事业务工具用声明出来）→ Task 2 标准库声明 + Task 4 dogfooding ✓
- spec §9 挂载点（createMcpServer 接生成工具，不改结构）→ Task 3 extraTools ✓
- spec §4 承重墙（写经正典原语）→ writeTool 已有 + Task 4 落库验证 ✓
- spec §8 第一个标准库包 → Task 2 narration.ts ✓
- spec §12 验收（扩展性零改动 + 安全 + 校验）→ Task 4 五测试覆盖落库/承重/坏参/零改动 ✓
- **OUT（显式不做，已在「设计张力」记录）**：团本 manifest tools 段 import（独立 plan）、完整真 LLM 一局（喂 F1）、front_advance（撞天花板记 backlog）

**2. Placeholder scan**：无 TBD；每个 code step 含完整代码与命令。

**3. Type consistency**：`toolgenToToolDef(decl: ToolDecl): ToolDef` 在 Task 1 定义、Task 2/4 使用一致；`narrationStdlibTools(): ToolDef[]` Task 2 定义、Task 3 测试/Task 4 使用一致；`wrapToolForTest(db, deps, extraTools?)` / `createMcpServer(db, deps?, extraTools?)` Task 3 定义、Task 4 使用一致；`{result}` 包装在 Task 1/2/4 一致。

**4. 承重墙验证闭环**：Task 4 验 front/plotline/foreshadow 落库经 store 函数（frontUpsert 等，由 writeTool executeInsert/executeSetStatus 调）——写永远经正典原语，不裸跑 SQL。

**5. DT-9 零改动**：Task 3 只加可选参数，现有 19 工具与 server.test 原测试不变（向后兼容由「默认无 extraTools」测试守）。
